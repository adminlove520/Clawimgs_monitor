
name: ClawFeeds Monitor

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      discussions: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Claw Monitor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_DISCUSSION_SWITCH: "ON"
          GH_DISCUSSION_OWNER: "ythx-101"
          GH_DISCUSSION_REPO: "openclaw-qa"
          GH_DISCUSSION_NUMBER: "133"
          DEDUP_SWITCH: "OFF"
          DINGDING_SWITCH: "OFF"
        run: python Rss_monitor.py --once

      - name: Update database
        if: always()
        run: |
          git config --global user.name 'ClawFeeds Bot'
          git config --global user.email 'bot@claw'
          git add articles.db 2>/dev/null || true
          git commit -m "Update database $(date +%Y-%m-%d\ %H:%M)" --allow-empty 2>/dev/null || true
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} main --force-with-lease
